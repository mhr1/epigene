#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[])
{
	FILE *popen();
	FILE *fp;
	char string[32];

	int x = 1, y = 1;
  /* First use the system() call. Output will go to stdout. */
  //system("date");
        if(x & !y) printf("Yes\n");
        else printf("No\n");



}

